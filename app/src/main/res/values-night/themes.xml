<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.TheoreticalFFScorer" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Primary brand color. -->
        <!-- Secondary brand color. -->
        <!-- Status bar color. -->
        <!-- Customize your theme here. -->
        <item name="android:colorButtonNormal">@color/white</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:colorBackground">@color/n3_blue</item>
    </style>

    <style name="Theme.TheoreticalFFScorer.Buttons">
        <item name="android:textColor">@color/n3_blue</item>
    </style>
</resources>