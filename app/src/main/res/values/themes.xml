<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.TheoreticalFFScorer" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Primary brand color. -->
        <!-- Secondary brand color. -->
        <!-- Status bar color. -->
        <!-- Customize your theme here. -->

        <item name="android:colorButtonNormal">@color/n3_blue_accent</item>
        <item name="android:textColor">@color/n3_blue_accent</item>
        <item name="android:colorBackground">@color/light_theme_color</item>

    </style>

    <style name="Theme.TheoreticalFFScorer.Buttons">
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="Theme.TheoreticalFFScorer.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.TheoreticalFFScorer.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.TheoreticalFFScorer.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="FullScreen" parent="Theme.TheoreticalFFScorer.NoActionBar">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

</resources>